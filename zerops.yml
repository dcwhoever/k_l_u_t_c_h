zerops:
  - setup: nodejs

    build:
      base: nodejs@20
      buildCommands:
        - npm i
      deployFiles:
        - .

    run:
      base: nodejs@20
      ports:
        - port: 3000
          httpSupport: true
      start: node index.js
